apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "auctor.serviceName" (dict "context" . "name" "execution-service") }}
  labels:
    {{- include "auctor.labels" (dict "context" . "name" "execution-service") | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "auctor.selectorLabels" (dict "context" . "name" "execution-service") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "auctor.selectorLabels" (dict "context" . "name" "execution-service") | nindent 8 }}
    spec:
      serviceAccountName: {{ include "auctor.serviceName" (dict "context" . "name" "execution-service") }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    {{- include "auctor.selectorLabels" (dict "context" . "name" "execution-service") | nindent 20 }}
                topologyKey: kubernetes.io/hostname
      containers:
        - name: execution-service
          image: "{{ .Values.image.registry }}/execution-service:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
            runAsNonRoot: true
            runAsUser: 1000
          ports:
            - containerPort: {{ .Values.services.execution.httpPort }}
              name: http
          env:
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: ENVIRONMENT
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: LOG_LEVEL
            - name: EXECUTION_DB_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: EXECUTION_DB_URL
            - name: EXECUTION_DB_USER
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: EXECUTION_DB_USER
            - name: EXECUTION_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "auctor.fullname" . }}-secrets
                  key: EXECUTION_DB_PASSWORD
            - name: EXECUTION_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "auctor.fullname" . }}-secrets
                  key: EXECUTION_JWT_SECRET
            - name: EXECUTION_GRPC_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: EXECUTION_GRPC_HOST
            - name: EXECUTION_GRPC_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: EXECUTION_GRPC_PORT
            - name: EXECUTION_REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: EXECUTION_REDIS_URL
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "auctor.fullname" . }}-config
                  key: OTEL_EXPORTER_OTLP_ENDPOINT
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.execution.httpPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.execution.httpPort }}
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
          resources:
            limits:
              cpu: {{ .Values.resources.limits.cpu | quote }}
              memory: {{ .Values.resources.limits.memory | quote }}
            requests:
              cpu: {{ .Values.resources.requests.cpu | quote }}
              memory: {{ .Values.resources.requests.memory | quote }}
